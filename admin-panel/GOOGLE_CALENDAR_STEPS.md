# Подключение Google Calendar API (Пошаговая инструкция)

## ✅ У вас уже есть:
- Firebase проект
- Google Authentication

## 🎯 Что нужно сделать:

---

## Шаг 1: Включить Google Calendar API в Firebase Console

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект **praniebrightwaw**
3. В левом меню: **Build** → **Authentication**
4. Вкладка **Sign-in method**
5. Найдите **Google** → кликните на него
6. Скопируйте **Web client ID** (понадобится)

---

## Шаг 2: Включить Calendar API в Google Cloud

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите проект **praniebrightwaw** (тот же, что у Firebase)
3. В меню слева: **APIs & Services** → **Library**
4. Найдите "**Google Calendar API**"
5. Нажмите **ENABLE**

---

## Шаг 3: Добавить Calendar scope в OAuth

1. В Google Cloud Console: **APIs & Services** → **OAuth consent screen**
2. Нажмите **EDIT APP**
3. Шаг 2 "Scopes":
   - Нажмите **ADD OR REMOVE SCOPES**
   - Найдите и отметьте:
     - `https://www.googleapis.com/auth/calendar.events`
   - Нажмите **UPDATE**
4. Нажмите **SAVE AND CONTINUE**

---

## Шаг 4: Установить библиотеки

```bash
cd admin-panel
npm install gapi-script
```

---

## Шаг 5: Создать файл .env.local

Создайте файл `admin-panel/.env.local`:

```env
VITE_GOOGLE_CLIENT_ID=ваш_web_client_id_из_шага_1
```

Замените `ваш_web_client_id_из_шага_1` на реальный Client ID.

---

## Шаг 6: Добавить .env.local в .gitignore

Откройте `admin-panel/.gitignore` и добавьте:

```
.env.local
```

---

## Шаг 7: Я создам файлы

Скажите "создай файлы", и я создам:

1. `src/utils/googleCalendar.ts` - функции для работы с Calendar API
2. `src/hooks/useGoogleCalendar.ts` - React hook для авторизации
3. Обновлю `Orders.tsx` для использования API

---

## Шаг 8: Первый запуск

1. Запустите админку: `npm run dev`
2. Откройте Orders
3. Кликните кнопку 📅
4. Появится окно авторизации Google (первый раз)
5. Разрешите доступ к календарю
6. Событие создастся автоматически!

---

## 🔐 Безопасность

- Client ID можно публиковать (он публичный)
- Авторизация происходит через Google OAuth
- Токены хранятся в браузере
- Доступ только к календарю авторизованного пользователя

---

## 💡 Важно

После настройки:
- **Первый раз:** Нужно авторизоваться (разрешить доступ)
- **Потом:** События создаются автоматически одним кликом
- **Каждый менеджер:** Авторизуется своим Google аккаунтом
- **События создаются:** В календаре того, кто авторизовался

---

## 🚀 Готовы?

Выполните Шаги 1-6, затем скажите "создай файлы", и я добавлю код!
