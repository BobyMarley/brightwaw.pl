import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <main>
      <h1>BrightHouse Homepage (Qwik migration in progress)</h1>
      <p>This route will replace legacy `/index.html` after visual parity.</p>
    </main>
  );
});
